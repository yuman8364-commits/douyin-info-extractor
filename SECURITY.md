# 安全说明

## 支持范围

当前维护版本为 `2.0.x`。更早版本没有完整的媒体/工作簿事务和发布隐私检查，建议升级后再处理重要数据。

## 报告安全问题

这是私有仓库。发现数据丢失、隐私泄露、任意文件覆盖、恶意工作簿处理或依赖安全问题时，请通过仓库的私有 Issue 描述：

- 受影响版本和 Windows 版本；
- 最短复现步骤；
- 预期行为与实际行为；
- 已脱敏的日志和文件结构；
- 是否仍有可恢复备份。

不要在 Issue、提交、截图或测试夹具中上传真实分享链接、本机用户名路径、个人工作簿、媒体内容、Cookie、访问令牌或其它凭据。

## 数据保护边界

- 媒体和封面先进入输出目录同盘的 `.staging`，下载时使用 `.part` 文件。
- 工作簿保存到临时副本并验证可以重新打开后才替换正式文件。
- 替换前会备份旧媒体、封面、必要的文案和工作簿。
- 提交失败会尝试恢复全部旧文件，并在日志和摘要中标记回滚结果。
- 文件被 Excel/WPS 占用时不会自动关闭任何 Office 进程。
- 发布包只能携带空白运行状态；检测到 URL、本机用户路径或非空输入缓存时构建失败。

这些机制不能替代常规备份。处理不可替代的数据前，仍建议把整个输出目录复制到其它位置。

## 明确不支持

- 导入 Cookie、账号登录态或浏览器凭据；
- 获取私密、好友可见或其它无权访问的作品；
- 绕过验证码、访问控制、地区限制或平台风控；
- 将平台返回的错误页面伪装成媒体文件；
- 未经授权传播或再利用受版权保护的内容。

如果后续需求涉及这些边界，应先进行合规和安全评审，而不是直接扩展抓取逻辑。
