# 安全说明

## 支持范围

当前维护版本为 `2.0.x`。更早版本没有完整的媒体/工作簿事务和发布隐私检查，建议升级后再处理重要数据。

## 报告安全问题

这是公开仓库。发现数据丢失、隐私泄露、任意文件覆盖、恶意工作簿处理或依赖安全问题时，请勿创建公开 Issue；优先使用仓库 `Security` 页中的私密漏洞报告入口，或通过可信的私下渠道联系维护者，并提供：

- 受影响版本和 Windows 版本；
- 最短复现步骤；
- 预期行为与实际行为；
- 已脱敏的日志和文件结构；
- 是否仍有可恢复备份。

不要在 Issue、提交、截图或测试夹具中上传真实分享链接、本机用户名路径、个人工作簿、媒体内容、Cookie、访问令牌或其它凭据。

## 数据保护边界

- 媒体和封面先进入输出目录同盘的 `.staging`，下载时使用 `.part` 文件。
- 工作簿保存到临时副本并验证可以重新打开后才替换正式文件。
- 替换期间会临时保管旧媒体、封面和必要的文案以支持失败回滚；“备份文件”默认关闭，勾选后才长期保留这些副本及工作簿备份。
- 提交失败会尝试恢复全部旧文件，并在日志和摘要中标记回滚结果。
- 删除记录先暂存目标及后续关联文件，Excel 删除和顺序前移成功后才清理；失败会恢复原编号。默认永久清理，勾选“备份文件”时保留到 `删除备份`。
- 用户主动提取、失败重试或刷新时，文件占用会触发全部 Excel/WPS 表格进程的强制关闭；其它未保存工作簿可能丢失。工具启动时不会自动联网或写表。
- 应用专用 `browser_profile/` 可能保存人工验证产生的 Cookie 和缓存，只能留在本机运行状态目录，不能提交或分享。
- 发布包只能携带空白运行状态；检测到 URL、本机用户路径、非空输入缓存或浏览器资料时构建失败。

这些机制不能替代常规备份。默认不保留成功替换或删除前的旧副本；处理不可替代的数据前，请勾选“备份文件”或把整个输出目录复制到其它位置。删除操作始终有二次确认，但未勾选备份时成功删除不可撤销。

## 明确不支持

- 导入日常浏览器 Cookie、账号登录态或浏览器凭据；应用只保存自身专用浏览器窗口中的人工验证状态；
- 获取私密、好友可见或其它无权访问的作品；
- 绕过验证码、访问控制、地区限制或平台风控；
- 将平台返回的错误页面伪装成媒体文件；
- 未经授权传播或再利用受版权保护的内容。

如果后续需求涉及这些边界，应先进行合规和安全评审，而不是直接扩展抓取逻辑。
