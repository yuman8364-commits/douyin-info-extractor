# 更新日志

本项目使用 `主版本.次版本.修订号` 的版本格式。

## 2.0.19 - 2026-09-02

- 粘贴与输入列表已有内容完全相同的抖音链接时，弹窗显示原序号并阻止重复添加。
- 输入区新增“删除当前链接”按钮、编辑菜单和 `Ctrl+Delete` 快捷键；删除第 N 条后仅将后续序号依次前移，前面序号不变。

## 2.0.18 - 2026-08-30

- 修复短链/作品页遇到连接、TLS、DNS、超时和 408/5xx 时被过早判定失败的问题，增加有上限的退避重试，并保留明确的验证码、403/429 和下架分类。
- 长链接直接解析作品 ID，Requests 网络链路持续失败时自动切换到应用专用浏览器网络；短链接也可由专用浏览器完成解析。
- 封面、图集和视频流增加同一地址的有限重试；网络链路失败时复用浏览器上下文下载媒体，仍使用临时文件、响应类型和完整字节数校验。
- 增加 TLS/连接异常、5xx、媒体流中断和浏览器网络兜底回归测试，并在界面显示网络链路切换状态。

## 2.0.17 - 2026-08-27

- “刷新记录”改为每检查完一条就立即原子写回 Excel，并实时更新界面中的标题、点赞、评论、状态和颜色，不再等整批结束后统一写入。
- 更新中及刚成功的记录显示绿色、失败记录显示红色；整批完成重新加载后正常与已恢复记录回到无色，失败状态继续常驻红色。
- 先前状态包含“失效”的链接若本次重新抓取成功，状态明确写为“已恢复”。

## 2.0.16 - 2026-08-27

- 主操作区新增常驻“删除选中记录”按钮，不再要求用户寻找右键菜单或键盘快捷键。
- 删除确认继续同步覆盖输入链接、Excel 行、视频/图集、封面和文案，后续记录及文件名整体前移；失败仍执行事务回滚。

## 2.0.15 - 2026-08-26

- 刷新记录时自动滚动到当前行并用浅绿色高亮；更新成功保留绿色，更新失败改为浅红色。
- 应用重新载入记录后按状态恢复高亮：`正常` 为绿色，异常状态为红色。

## 2.0.14 - 2026-08-26

- 根据 26 日第 1 条真实失效链接修正浏览器落点识别：支持 `/jingxuan?previous_page=web_video_404_link&modal_id=...`。
- `web_video_404_link` 明确归类为目标作品失效，`modal_id` 作为跳转后的推荐作品 ID；不再误报风控或验证异常。
- 使用该真实链接和正式专用浏览器资料完成只读验收，完整引擎在 10 秒观察后返回 `TargetUnavailableError`。

## 2.0.13 - 2026-08-26

- 浏览器跨作品跳转的失效判定改为打开目标页后观察满 10 秒再确认，减少页面初始化期间短暂跳转造成的误判。
- 部署流程按用户要求不再为本次更新创建额外正式目录备份；既有备份保持不动。

## 2.0.12 - 2026-08-26

- 专用浏览器打开目标作品后若自动跳转到其他视频或图文 ID，立即判定原作品已失效，不再等待浏览器验证超时。
- 浏览器确认失效后不执行 HTTP/浏览器重试，不触发批次风控熔断；只更新当前记录状态并继续检查后续记录。
- 登录页、验证码页等不包含其他作品 ID 的跳转不会被误判为作品失效。

## 2.0.11 - 2026-08-26

- 修复刷新记录收到浏览器验证/链接异常消息时界面轮询器变量名错误，避免界面无响应且任务状态无法复位。
- 刷新任务在关闭专用浏览器和 Playwright 后才恢复按钮；清理尚未结束时拒绝启动第二个刷新任务，避免浏览器资料目录争用造成卡住。
- 新增界面消息显示和浏览器清理顺序回归测试。

## 2.0.10 - 2026-08-25

### 风控与浏览器兜底

- 精确识别 HTTP 200 的抖音验证码中间页，命中后立即停止 HTTP 重试，避免整批请求放大。
- 正常请求继续使用 HTTP；验证码或页面结构变化时弹出应用专用 Edge/Chrome 会话，允许用户人工完成验证后继续。
- 浏览器验证失败会暂停整批，只更新当前已检查记录；后续未检查记录保持原状态和更新时间。
- 取消启动自动刷新，只有用户主动提取或刷新时才访问网络。
- 发布包不包含应用浏览器资料、Cookie 或缓存。

## 2.0.9 - 2026-08-23

### 修复

- 修复已有作品抓取失败后仍保留“正常”状态的问题。
- 刷新或提取已有链接失败时，旧标题、互动数和人工字段继续保留，但状态与最后更新时间会写入本次检查结果。

## 2.0.8 - 2026-08-23

### 界面

- 主操作按钮“强制刷新记录”更名为“刷新记录”，原有抓取和写表行为不变。
- 新增“打开提取记录”按钮，直接打开当前输出目录中已有的 `提取记录.xlsx`；文件不存在时只提示，不创建空表。

## 2.0.7 - 2026-08-21

### 同步删除

- 下方记录支持右键菜单、Delete 键和“编辑”菜单删除。
- 二次确认后同步删除上方匹配链接、下方列表、Excel 行、视频/图集、封面及文案。
- 删除中间记录会把后续 Excel 顺序和关联文件编号整体前移，避免输入、表格和文件错位。
- 文件重排或写表失败时恢复原文件；勾选“备份文件”时，被删除文件保留到 `删除备份`。
- 删除写表遇占用时沿用强制关闭 Excel/WPS 后重试的用户设置。

## 2.0.6 - 2026-08-20

### 备份选项

- 增加默认不勾选的“备份文件”选项。
- 未勾选时不新增长期的 `表格备份` 和 `替换备份`；失败回滚仍使用临时中转保护旧文件。
- 勾选后恢复长期保留工作簿及被替换媒体、封面和文案副本的行为；已有备份不会被自动清理。

## 2.0.5 - 2026-08-20

### 写表行为

- 用户主动提取、失败重试或手动强刷遇到 `提取记录.xlsx` 被占用时，自动强制关闭全部 Excel/WPS 表格进程并重试写入。
- 启动后台自动刷新保持非侵入行为，不会仅因打开工具就强制关闭 Office。

## 2.0.4 - 2026-08-20

### 行为

- “全部提取”和“单次提取”遇到表格已有记录时，恢复旧版分流：只强制刷新并原子写回 Excel，即使媒体缺失也不进入下载。
- 任务状态、完成摘要和日志统一显示“强制刷新写表”，不再把已写表的记录标成“跳过”。

### 文档与授权

- 项目标题改为“抖音提取工具｜Douyin Info Extractor”。
- 添加 MIT 开源许可证，版权所有者标注为 `yuman8364-commits`。

## 2.0.3 - 2026-08-19

### 修复

- 修复从旧工作簿导入链接后因日志器尚未初始化而弹出异常的问题。
- 恢复启动时对输入缓存中“已有记录且媒体仍存在”的条目直接刷新公开元数据的行为。
- 手动更新入口改名为“强制刷新记录”，明确它不会重新下载媒体。

### 验证

- 22 项自动化测试通过。
- 使用 PowerShell 7.6.5 重建 Windows 发布版。
- 发布隐私扫描、全新目录启动、旧文档只读导入和固定封面预览验证通过。

## 2.0.2 - 2026-08-19

### 新增

- 支持从以前生成的 `xlsx/xlsm` 中按“顺序”列导入链接并回填输入框。
- 导入过程只读、不联网、不修改源文档；取消或读取失败时保留当前输入。

### 修复

- 封面预览改为固定容器和固定画布，保持比例居中显示，避免选中视频时挤压右侧界面。

## 2.0.1 - 2026-08-19

### 新增

- 增加失败项重试、停止任务、安全退出和结构化任务消息。
- 增加日志轮转、诊断摘要、任务期间输入锁定和正确的图集张数进度。
- 构建使用隔离虚拟环境和锁定版本，发布目录带版本号与构建时间。

## 2.0.0 - 2026-08-19

### 安全整改

- 媒体、封面和 Excel 统一采用暂存、校验、提交与失败回滚流程。
- 工作簿先写临时副本并验证可重开，再原子替换且保留最近备份。
- 删除自动结束所有 Excel/WPS 进程的行为；文件被占用时只提示用户处理。
- 发布构建不再复制开发机配置和输入缓存，并增加隐私扫描。

### 数据模型

- 引入包含成功请求 Session 的 `FetchedRecord`。
- 解析结果必须核对 `aweme_id`，去重主键改为作品 ID。
- 新增“作品ID”“作品形式”“最后更新”三列。
- 按表头名称迁移旧表，仅更新受管理列并保留额外工作表与人工内容。
